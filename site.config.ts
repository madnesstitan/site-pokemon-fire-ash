// Auto-generated by sita (sita/pipeline/design.py). Re-run `sita build`
// to change; hand-edits will be overwritten on the next run.

export type ThemeName = 'slate' | 'paper' | 'bold' | 'niche';

export interface SiteConfig {
  name: string;
  domain: string;
  theme: ThemeName;
  accent: `#${string}`;
  niche: string;
  tagline?: string;
}

export const site: SiteConfig = {
  name: "Pokemon Fire Ash",
  domain: "pokemon-fire-ash.pages.dev",
  theme: "niche",
  accent: "#38bdf8",
  niche: "Pokemon Fire Ash",
  tagline: "Your complete resource for the fan-made RPG that follows Ash Ketchum's anime journey",
};

/** Full canonical origin, used by astro.config.mjs and BaseLayout. */
export const siteUrl = `https://${site.domain}`;
